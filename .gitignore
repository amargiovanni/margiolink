# Secrets — never commit
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# Dependencies
node_modules/

# Build output
dist/
build/
.wrangler/
web/dist/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log

# Worktrees
.claude/worktrees/

# Generated by the team-onboarding command for sharing outside this repo.
# It carries an instruction block addressed to an AI assistant, which has no
# business executing from inside a source tree — it was swept into a commit
# once by `git add -A` and removed in the next.
ONBOARDING.md
